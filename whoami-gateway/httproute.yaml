apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whoami
spec:
  parentRefs:
    - name: lab-gateway
      namespace: default
  hostnames:
    - whoami.lab.local
  rules:
    - backendRefs:
        - name: whoami
          port: 80
